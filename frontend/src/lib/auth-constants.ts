/** Cookie name for JWT access token — must match middleware and auth-utils. */
export const AUTH_TOKEN_COOKIE = "cg_access_token";

/** JSON user payload (URL-encoded) — readable by Server Components via cookies(). */
export const AUTH_USER_COOKIE = "cg_auth_user";
